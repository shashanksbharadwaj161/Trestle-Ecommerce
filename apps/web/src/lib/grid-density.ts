/** Listing grid density — shared by the server page (reads the cookie) and the client toggle (writes it). */
export type Density = "standard" | "large";
export const DENSITY_COOKIE = "trestle_grid";
