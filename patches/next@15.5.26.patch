diff --git a/dist/compiled/react-dom/cjs/react-dom-client.development.js b/dist/compiled/react-dom/cjs/react-dom-client.development.js
index 4376913076151ad78dcc4059ad4152fad25478da..d743b2dca496de859a84a0459e4af25320775c63 100644
--- a/dist/compiled/react-dom/cjs/react-dom-client.development.js
+++ b/dist/compiled/react-dom/cjs/react-dom-client.development.js
@@ -15776,6 +15776,14 @@
           break;
         case 5:
           resetHooksOnUnwind(unitOfWork);
+          // backport of React 19.3 (see production build)
+          if (unitOfWork === hydrationParentFiber)
+            isHydrating
+              ? (popToNextHostParent(unitOfWork),
+                5 === unitOfWork.tag &&
+                  null != unitOfWork.stateNode &&
+                  (nextHydratableInstance = unitOfWork.stateNode))
+              : (popToNextHostParent(unitOfWork), (isHydrating = !0));
         default:
           unwindInterruptedWork(current, unitOfWork),
             (unitOfWork = workInProgress =
diff --git a/dist/compiled/react-dom/cjs/react-dom-client.production.js b/dist/compiled/react-dom/cjs/react-dom-client.production.js
index 755076a5cf15273f024c18fdc6a8805f1e36860b..7df268db26abedf881806e7fbfcd35a1eb9b9850 100644
--- a/dist/compiled/react-dom/cjs/react-dom-client.production.js
+++ b/dist/compiled/react-dom/cjs/react-dom-client.production.js
@@ -11210,6 +11210,13 @@ function replaySuspendedUnitOfWork(unitOfWork) {
       break;
     case 5:
       resetHooksOnUnwind(next);
+      // backport of React 19.3: a host component replayed while hydrating must rewind the hydration cursor to
+      // its own DOM node, or it claims its first child instead and hydration fails (#418)
+      if (next === hydrationParentFiber)
+        isHydrating
+          ? (popToNextHostParent(next),
+            5 === next.tag && null != next.stateNode && (nextHydratableInstance = next.stateNode))
+          : (popToNextHostParent(next), (isHydrating = !0));
     default:
       unwindInterruptedWork(current, next),
         (next = workInProgress =
diff --git a/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js b/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
index 85185b07d38d122ada8ed308523b181da102ccad..9681d1a281495cd9aa88f748bc916a0ea927ca27 100644
--- a/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
+++ b/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
@@ -11814,6 +11814,13 @@ function replaySuspendedUnitOfWork(unitOfWork) {
       break;
     case 5:
       resetHooksOnUnwind(next);
+      // backport of React 19.3: a host component replayed while hydrating must rewind the hydration cursor to
+      // its own DOM node, or it claims its first child instead and hydration fails (#418)
+      if (next === hydrationParentFiber)
+        isHydrating
+          ? (popToNextHostParent(next),
+            5 === next.tag && null != next.stateNode && (nextHydratableInstance = next.stateNode))
+          : (popToNextHostParent(next), (isHydrating = !0));
     default:
       unwindInterruptedWork(current, next),
         (next = workInProgress =
